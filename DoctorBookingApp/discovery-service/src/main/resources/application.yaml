# Configuration file for discovery-service
server:
  port: 8761
spring:
  application:
    name: discovery-service
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false